import { LicationAndNamePipe } from './lication-and-name.pipe';

describe('LicationAndNamePipe', () => {
  it('create an instance', () => {
    const pipe = new LicationAndNamePipe();
    expect(pipe).toBeTruthy();
  });
});
