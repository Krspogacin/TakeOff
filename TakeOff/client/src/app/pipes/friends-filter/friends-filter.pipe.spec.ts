import { FriendsFilterPipe } from './friends-filter.pipe';

describe('FriendsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FriendsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
